{
  "buildCommand": "pip install -r requirements.txt",
  "startCommand": "python -m uvicorn algo_dashboard_backend:app --host 0.0.0.0 --port $PORT",
  "env": {
    "PYTHONUNBUFFERED": "1",
    "PYTHONDONTWRITEBYTECODE": "1"
  }
}
